Fix Wiring Pi makefile
--- a/wiringPi/Makefile
+++ b/wiringPi/Makefile
@@ -59,7 +59,7 @@
 
 OBJ	=	$(SRC:.c=.o)
 
-all:		$(DYNAMIC)
+all:		$(DYNAMIC) static
 
 static:		$(STATIC)
 
@@ -80,7 +80,7 @@
 .PHONY:	clean
 clean:
 	@echo "[Clean]"
-	@rm -f $(OBJ) $(OBJ_I2C) *~ core tags Makefile.bak libwiringPi.*
+	@rm -f $(OBJ) $(OBJ_I2C) *~ core tags Makefile.bak libwiringPi.a* libwiringPi.so*
 
 .PHONY:	tags
 tags:	$(SRC)
@@ -117,12 +117,14 @@
 	@install -m 0644 sn3218.h		$(DESTDIR)$(PREFIX)/include
 
 .PHONY:	install
-install:	$(DYNAMIC) install-headers
+install:	$(DYNAMIC) install-static install-headers
 	@echo "[Install Dynamic Lib]"
 	@install -m 0755 -d						$(DESTDIR)$(PREFIX)/lib
 	@install -m 0755 libwiringPi.so.$(VERSION)			$(DESTDIR)$(PREFIX)/lib/libwiringPi.so.$(VERSION)
-	@ln -sf $(DESTDIR)$(PREFIX)/lib/libwiringPi.so.$(VERSION)	$(DESTDIR)/lib/libwiringPi.so
-	@ldconfig
+	@ln -sf $(DESTDIR)$(PREFIX)/lib/libwiringPi.so.$(VERSION)	$(DESTDIR)$(PREFIX)/lib/libwiringPi.so
+	@ldconfig -n
+	@install -m 0755 -d						$(DESTDIR)$(PREFIX)/lib/pkgconfig
+	@install -m 0644 wiringPi.pc					$(DESTDIR)$(PREFIX)/lib/pkgconfig
 
 .PHONY:	install-static
 install-static:	$(STATIC) install-headers
@@ -157,6 +159,7 @@
 	@rm -f $(DESTDIR)$(PREFIX)/include/pcf8591.h
 	@rm -f $(DESTDIR)$(PREFIX)/include/sn3218.h
 	@rm -f $(DESTDIR)$(PREFIX)/lib/libwiringPi.*
+	@rm -f $(DESTDIR)$(PREFIX)/lib/pkgconfig/wiringPi.pc
 	@ldconfig
 
 
--- /dev/null
+++ b/wiringPi/wiringPi.pc
@@ -0,0 +1,10 @@
+prefix=/usr
+exec_prefix=${prefix}
+libdir=${prefix}/lib
+includedir=${prefix}/include
+
+Name: libwiringPi
+Description: GPIO Interface library for the Raspberry Pi
+Version: 2.0
+Libs: -L${libdir} -lwiringPi
+Cflags: -I${includedir}
